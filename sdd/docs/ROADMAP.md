# ROADMAP — Guía unificada de mejoras del ecosistema SDD

**Creado:** 2026-06-05
**Fuentes consolidadas (ya eliminadas tras la consolidación):** auditoría profunda 2026-06-05 (5 ejes: pipeline core, design, meta/instalación, tech, adopción) + `IMPROVEMENTS.md` (mejoras gentle-ai y backlog A/B/C) + `GENTLE_AI_COMPARISON.md` (persistencia/memoria) + `audit_full_global.md` (audit 2026-05-31, estado re-verificado a fecha de hoy).

**Cómo usar este documento:** es la SSoT única del trabajo pendiente sobre el ecosistema. Cada ítem tiene checkbox, severidad y evidencia. Al completar un ítem: marcar `[x]`, anotar fecha y commit. Cuando una sección quede completa, moverla a "Completado" al final.

**Leyenda severidad:** 🔴 crítico · 🟠 alto · 🟡 medio · 🟢 bajo

---

## FASE 0 — Bugs concretos (arreglables ya, días no semanas) — ✅ COMPLETADA 2026-06-05

Errores reproducibles, no decisiones de diseño. Los 10 ítems cerrados el 2026-06-05 (detalle de verificación en cada uno).

- [x] 🔴 **0.1 — `tech/kmm/install.sh` no copia `kb-kmm-project-state-protocol`.** La KB vive en la raíz de `skills/` y los bucles solo iteran `plan`, `tasks` y `wf-*`. Es precondición dura de 6 agentes ("si `kmm_project_state.md` no existe… detente"): el stack llega roto de fábrica. → Añadir la raíz de `skills/` (o la KB explícita) al installer. ✅ *2026-06-05: añadido bucle `kb-*/` de raíz en `tech/kmm/install.sh`; verificado con instalación real en tmpdir (42 skills, KB desplegada, 6 agentes consumidores OK).*
- [x] 🔴 **0.2 — Variante `optimized` de `kmm/install.sh` rota.** Referencia `agents_optimized/`, `skills_optimized/`, `CLAUDE_optimized.md` — ninguno existe; con `set -e` peta a medias dejando el proyecto inconsistente (`tech/kmm/install.sh:20-22`). → Eliminar la variante o crear los directorios. ✅ *2026-06-05: variante eliminada por completo (case, SKILL_VARIANT y uso); el script ya no acepta argumento. Verificado: ningún invocador la pasaba; instalación en tmpdir OK (42 skills, 9 agentes).*
- [x] 🟠 **0.3 — `install.sh` sobreescribe `.claude/settings.json` del proyecto sin merge** (`install.sh:213`), y `tech/kmm/install.sh:90` lo vuelve a pisar. Destruye permisos/hooks propios del equipo. `setup.sh:52-77` ya tiene un merge idempotente con python3 para el global — replicar ese patrón a nivel proyecto. ✅ *2026-06-05: creado helper compartido `sdd/scripts/merge-claude-settings.py` (SSoT del merge: añade matchers ausentes por evento, preserva todo lo del proyecto, idempotente) usado por ambos installers; fallback seguro sin python3 (no modifica, avisa). Verificado: 3 escenarios (sin settings previo, settings del equipo preservados, re-run + overlay KMM sin duplicados).*
- [x] 🟠 **0.4 — `wf-design-validate`: flag `--strict` inyectado al agente nunca se parsea.** El frontmatter declara `--lenient`/`--pedagogical` pero el prompt del Paso (línea ~76) inyecta `Flag --strict: <true|false>` que el Paso 1 no llena. Además semántica invertida respecto a `wf-design-a11y-audit` (`--lenient` opt-out vs `--strict` opt-in). → Unificar semántica de severidad entre ambos y corregir el prompt. ✅ *2026-06-05: semántica unificada a estricto-por-defecto + `--lenient` opt-out en ambos workflows (coherente con la filosofía declarada de la fase). El prompt de validate ahora pasa `--lenient` y `--pedagogical` reales + el contenido de `--views` (que se parseaba pero nunca llegaba al agente). `--strict` eliminado del ecosistema (skill, rootmap de CLAUDE.md, design_md_template); reglas de Estado añadidas al Paso 9 de a11y-audit. Verificado: grep sin coincidencias de `--strict`.*
- [x] 🟠 **0.5 — `skill-registry.md` desincronizado y sin mecanismo de regeneración.** Header con totales/fecha obsoletos (`meta/skill-registry.md:3-4`); nada lo regenera al crear/borrar skills. → Mínimo: que `wf-skill-create`/`wf-agent-create`/`wf-stack-create` regeneren el registry al cerrar (o invoquen el paso de `wf-sdd-status`). Definir autoridad única de regeneración (hoy `wf-sdd-status` y `wf-sdd-audit` solapan). ✅ *2026-06-05: creado generador determinista `sdd/scripts/generate-skill-registry.py` (SSoT de generación: escanea el filesystem — el registry ya no puede mentir). Lo ejecutan `wf-sdd-status` (Paso 6.5), `wf-skill-create` (Paso 8), `wf-stack-create` y `wf-sdd-refactor` al cerrar; Regla 18, meta/CLAUDE.md e inventory_templates actualizados (plantilla manual eliminada para no duplicar SSoT). `wf-agent-create` no lo necesita: el registry solo contiene skills. Registry regenerado: el header decía 95 skills, hay 106. Verificado: idempotente, cero referencias al mecanismo antiguo.*
- [x] 🟡 **0.6 — Parseo frágil de `project-init.json` en el hook.** `bootstrap/sdd-session-check.sh:31` extrae `phases` con `grep -o` + glob de substring; un reformateo manual da falsos `init-incomplete` (muy intrusivo: relanza `wf-project-init` cada sesión). → Parsear con python3 (con fallback) o endurecer el matching. ✅ *2026-06-05: parser primario python3 (JSON real), fallback textual endurecido (tokens exactos, no substring) sin python3, y comportamiento conservador ante JSON malformado (silencio, no init-incomplete espurio). Verificado: 6 casos camino python3 + 5 casos pipeline fallback, todos PASS (multilinea, claves con substring engañoso, malformado, phases no-lista). ✓ Resuelto 2026-06-07: copia instalada sincronizada con `setup.sh --update` (verificado en sesión: hook idéntico al del repo).*
- [x] 🟡 **0.7 — El hook no excluye el repo contenedor del ecosistema.** Solo excluye `$SDD_HOME` (= `…/ai-dev-agents-lab/sdd`); abrir sesión en `ai-dev-agents-lab/` dispara el wizard (ocurrió el 2026-06-05). → Excluir también el repo git que contiene `$SDD_HOME` (p. ej. `git -C "$SDD_HOME" rev-parse --show-toplevel`). ✅ *2026-06-05: añadida exclusión del toplevel git que contiene SDD_HOME, comparando rutas físicas (`pwd -P`) porque git resuelve symlinks; degrada con gracia si git no está disponible. Verificado: 6 casos con repo simulado (SDD_HOME, repo contenedor, subdir, control de proyecto normal, hermano con prefijo similar, repo real). ✓ Resuelto 2026-06-07: hook re-sincronizado con `setup.sh --update`.*
- [x] 🟡 **0.8 — Reinstalar con menos fases deja skills huérfanas invocables.** `install.sh:126` hace `rm -rf`+`cp` solo de lo que instala; nunca limpia lo que ya no aplica. → Modo de reconciliación: eliminar skills/agents de fases no seleccionadas (con confirmación). ✅ *2026-06-05: añadida reconciliación a `install.sh` — detecta piezas SDD (skills y agents) presentes en `.claude/` que pertenecen a fases no seleccionadas; sin flag las lista con aviso (no destructivo, la "confirmación" es re-ejecutar con `--prune`); con `--prune` las elimina. Solo toca piezas del framework: skills propias del proyecto y overlays de stack (tech/<stack>) intactos. Verificado: ciclo all→prd,spec (aviso) → --prune (35 piezas eliminadas, propias y KMM intactas) → re-run idempotente → install fresco sin avisos.*
- [x] 🟡 **0.9 — `setup.sh` depende de `python3` sin verificar** (`setup.sh:52`); en macOS sin python3 falla a mitad dejando el bootstrap inconsistente (hook copiado pero sin registrar). → `command -v python3` + fallback o mensaje claro y rollback del paso. ✅ *2026-06-05: precheck fail-fast de python3 al inicio de `setup.sh`, ANTES de modificar nada — error claro con instrucciones de instalación (macOS/Linux) y exit 1 limpio. No hace falta rollback porque no llega a tocar nada. Verificado en HOME falso: sin python3 → exit 1 + HOME intacto; con python3 → setup completo (skill, .sdd-home, hook, registro en settings, bloque CLAUDE.md) + re-run idempotente sin duplicados.*
- [x] 🟢 **0.10 — `sdd/.claude/settings.local.json` trackeado en git con paths personales.** El `.gitignore` raíz ignora `sdd/.claude/` pero el fichero quedó dentro. → `git rm --cached` + reforzar gitignore. ✅ *2026-06-05: destrackeado con `git rm --cached` (fichero intacto en disco como workspace local); verificado que `.gitignore:5` ya lo cubre y no puede reentrar con `git add` normal. Nota: permanece en el historial de commits previos — son permisos de sesión con paths, no secretos, así que no justifica reescribir historia.*

---

## FASE 1 — Enforcement mecánico (el riesgo central del sistema)

Hoy todo el rigor es prosa que el modelo debe obedecer; los gates son auto-aprobables. Convertir lo "obligatorio" en verificable.

- [x] 🔴 **1.1 — Hooks `PreToolUse` para gates de fase.** Bloquear mecánicamente: `wf-prepare-tasks` sin `_plan.md` con `Estado: VALIDADO` (grep determinista), `wf-prepare-plan` sin spec en `status_sync: in_sync`, `wf-design-feature-prototype` sin spec validado. Los `!test -f` actuales solo verifican existencia, nunca contenido. ✅ *2026-06-05: creado `sdd/scripts/sdd-gate-check.py` — hook PreToolUse (matcher Skill) con tabla de gates data-driven: `wf-prepare-tasks`→plan VALIDADO; `wf-prepare-plan`/`wf-design-system`/`wf-design-feature-prototype`→spec sin `[INCOMPLETO]`/`[CRÍTICO]` y `status_sync` fiable. Deniega vía `permissionDecision: deny` con motivo accionable (`[SDD-GATE] ...ejecuta /wf-plan-validate`). Política conservadora: solo bloquea con evidencia positiva (path no resoluble → permite; stdin malformado → permite; error interno → permite). Registrado en `settings.json` (base y kmm) con wrapper defensivo (sin python3 o sin script → exit 0, nunca bloquea por accidente); `install.sh` distribuye el script a `.sdd/scripts/`. El brief de design queda fuera del gate mecánico (ubicación ambigua desde args — sigue en la prosa del workflow). Verificado: matriz de 11 casos hook-protocol + e2e con install y merge sobre settings de equipo existentes. Nota: compensa el auto-allow de `Skill(.*)` señalado en 1.4.*
- [x] 🔴 **1.2 — Separar autor de auditor en los sellados.** `wf-plan-validate` = el agente emite "OK" en prosa y el orquestador hace find-replace `BORRADOR→VALIDADO`. Mínimo: que el sellado lo haga un script determinista que verifique condiciones comprobables (CAs del spec presentes en el plan por ID, cero `[INCOMPLETO]`), no el mismo flujo que generó el artefacto. ✅ *2026-06-05: creado `sdd/scripts/sdd-seal.py` (modos `--check/--seal/--unseal`) — verifica 7 condiciones mecánicas: estructura de plan, 4 líneas de gaps en `ninguno`, sin `[INCOMPLETO]`, spec origen legible, spec sin `[INCOMPLETO]`/`[CRÍTICO]`, `status_sync: in_sync`, y cobertura de todos los CA-XXX del spec en el plan. El veredicto OK del agente ya no basta: si la verificación falla, fuerza `BORRADOR` y muestra qué condición falló. `install.sh` lo distribuye a `.sdd/scripts/` del proyecto (committeable, funciona en CI); `wf-plan-validate` Paso 5 reescrito (ya no edita `Estado:` a mano, +Bash en allowed-tools); SSoT de estados en `kb-plan-expert` actualizada. Verificado: matriz de 8 escenarios + 5 variantes de formato de header + end-to-end con install y paths relativos al plan. Pendiente conectar spec/design al mismo patrón cuando se mecanicen sus gates (1.1).*
- [x] 🟠 **1.3 — Detección automática de deriva PRD→spec.** `status_sync` es manual y miente en silencio si alguien edita el PRD fuera de `wf-prd-change`. → Sellar hash/mtime del PRD origen en la metadata del spec (`derived_from_prd_version` ya existe — hacerlo verificable) y un check barato que marque `stale` al detectar divergencia (puede ir en el hook SessionStart o en los gates). ✅ *2026-06-07: (a) nuevo script de enforcement `sdd/scripts/sdd-sync-check.py` — `seal` calcula sha256 del PRD (normalizado CRLF→LF, prefijo 16 hex) resuelto desde `derived_from_prd` (relativo al spec, fallback cwd) y escribe `derived_from_prd_hash` en el header; único escritor del campo, mismo principio autor≠verificador del sellador. `check`/`check-all [--mark]` recomputan y reportan IN_SYNC/DERIVA/SIN_SELLO/NO_APLICA/PRD_NO_RESUELVE; `--mark` degrada `status_sync` a `needs_review` — no `stale`: el mismatch prueba que el PRD cambió, no que ESTA feature esté afectada; el impacto por feature lo decide sync-impact (semántica documentada en `kb-traceability-rules`, sección nueva del campo verificable). (b) Enforcement en ambos verificadores: `gate_spec_fiable` (sdd-gate-check.py) deniega `wf-prepare-plan`/`wf-design-*` ante deriva, y `sdd-seal.py` añade el check 6b (un plan no se sella contra un spec con deriva — y un plan ya VALIDADO se degrada a BORRADOR al re-checkear). Política conservadora intacta: sin sello (specs legacy), PRD `N/A` o no resoluble → no bloquea. (c) Selladores: `wf-spec-fast-track` Paso 10 y `wf-spec-sync-from-prd` Paso 4B ejecutan `seal` tras escribir; `wf-prd-sync-impact` y `sync-from-prd analyze` hacen pre-pass determinista `check-all --mark` antes del análisis experto. Templates de header actualizados (shared SSoT + redistribución a references). Decidido: el check vive en los gates, no en el hook SessionStart (targeted, cero ruido por sesión). Verificado: matriz de 13 casos del script + 7 del gate/sellador (deny por deriva, plan degradado, resync desbloquea, legacy silencioso) + install e2e. Versión 0.2.0 con avisos ⚠ en CHANGELOG.*
- [x] 🟠 **1.4 — Revisar el auto-allow universal de Skills.** `settings.json` aprueba `Skill(.*)`: elimina toda fricción incluso para saltarse fases. → Evaluar lista explícita o combinarlo con los hooks de 1.1 (el gate compensa el auto-allow). ✅ *2026-06-07: auto-allow acotado — el `Skill(.*)`+echo universal sustituido por `sdd-skill-allow.py` (hook PermissionRequest, distribuido a `.sdd/scripts/`): solo auto-aprueba workflows SDD (`wf-*`, regex estricta, tolera forma `plugin:skill`); `kb-*` y skills ajenas al ecosistema siguen el flujo normal de permisos. Los gates PreToolUse de 1.1 se evalúan ANTES y su deny prevalece: el allow no puede saltarse fases. Wrapper defensivo idéntico al del gate (sin python3 o sin script → sin decisión y el harness pregunta — degrada a fricción, nunca a permisividad). Migración para proyectos ya inicializados: `merge-claude-settings.py` elimina la entrada antigua por comando EXACTO (`DEPRECATED_HOOK_COMMANDS` — hooks propios del equipo con el mismo matcher quedan intactos) antes del merge, idempotente. Aplicado en settings base y overlay KMM. Verificado: 8 casos del script + merge sobre settings viejo conservando hook del equipo + install e2e con migración real.*
- [x] 🟡 **1.5 — KB Load Status verificable.** El "KB Load Status" es auto-reportado por el propio agente. → Que el orquestador verifique la existencia de las KBs declaradas antes de delegar (check determinista), dejando el auto-reporte solo como señal secundaria. ✅ *2026-06-08 (cierra la Fase 1): nuevo script determinista `sdd-kb-check.py` — lee el frontmatter `skills:` de cada agente instalado y verifica que cada KB existe como `.claude/skills/<kb>/SKILL.md` (`--all`/`--agent`, exit 2 si falta, soporta forma inline y multilínea). **Granularidad decidida**: el set de KBs de un agente es fijo (su frontmatter) y no cambia entre delegaciones, así que el punto eficiente es instalación + arranque, no "antes de cada delegación" (redundante). `install.sh` y el overlay KMM lo ejecutan al cerrar (no bloqueante): avisan de instalaciones incompletas, dependencias cross-phase no traídas u overlays no re-aplicados. **Regla 19 de `kb-sdd-skill-architecture`** reescrita: el check determinista es la señal autoritativa, el `## KB Load Status` auto-reportado queda secundario (reflejado en `body-templates.md`). Hilo "verdad mecánica sobre auto-reporte" coherente con sellador/gates/features-index/project-status. VERSION 0.12.0 + CHANGELOG con ⚠. Verificado: OK/faltante/--agent/multilínea en tmpdir + `.claude/` real del repo + e2e install (script estampado, verificación corre sola → 5 agentes spec OK, valida resolución cross-phase de kb-prd-expert/kb-a11y-expert). Sin skill nueva → registry intacto.*
- [x] 🟡 **1.6 — Red anti-fabricación en el origen (PRD).** `wf-prd-create` sin `--source` puede inventar actores/alcance desde un brief pobre y `wf-prd-review` solo valida estructura. → Exigir que cada afirmación de negocio trace a la fuente o quede marcada `[ASUNCIÓN]` con confirmación humana explícita en el review. ✅ *2026-06-08: (a) **SSoT = Regla 12 nueva de `kb-prd-expert`**: toda afirmación de negocio (actor, capacidad, exclusión, regla, objetivo) traza al material fuente / al brief, o se marca `[ASUNCIÓN]` inline + recopilación en `## Asunciones del PRD` con `[ASN-XXX]` (afirmación + hueco que rellena + casilla). Prohibida la invención silenciosa (ante la duda, se marca). IDs propios sin colisión con `[P-XXX]`/`[A-XXX]`; tabla asunción≠gap (asunción = contenido inventado a confirmar en review; gap = pregunta abierta del pipeline). (b) `wf-prd-create` Pasos 4/6/7: instruye la regla al `prd-expert` (énfasis agresivo sin `--source`), añade la sección a la salida y reporta el nº de asunciones. (c) `wf-prd-review` **Paso 5.5 — gate de confirmación humana**: check determinista (`grep "[ASUNCIÓN]"`), presenta cada `[ASN-XXX]` con `AskUserQuestion` (confirmar/rechazar/editar) y edita el PRD solo según la decisión del usuario; **asunciones sin confirmar ⇒ no `LISTO`** (sección nueva en el veredicto). Es la única edición que el review hace del documento — acotada. `AskUserQuestion` añadido a su `allowed-tools`. (d) `prd-expert` (agente) alinea flujo de creación + regla de oro con la Regla 12; `kb-gap-conventions` acota su ámbito a Spec-en-adelante y añade puntero cruzado al marcador del PRD (sin absorber el de otra fase). Cambio prosa/KB puro — sin scripts ni `install.sh` nuevos (el enforcement mecánico es el `grep` dentro del review). VERSION 0.10.0 + CHANGELOG con ⚠. Verificado: coherencia de marcadores en las 5 piezas, sin colisión de IDs, Regla 12 referenciada en las 4 piezas PRD.*

---

## FASE 2 — Ciclo de vida completo (de "documentador" a "entregador")

El pipeline hoy termina en `_tasks.md` y solo conoce "feature nueva". Más del 50% del trabajo real de una empresa queda fuera.

- [x] 🔴 **2.1 — `wf-task-run` — ejecución de tasks con estado persistente** en `_tasks.md`, validación de DoD por task y commits trazables (`T-00X`, `CA-XXX`). Cierra el ciclo spec→plan→tasks→implementación. *(= B1 de IMPROVEMENTS)* ✅ *2026-06-07: (a) nuevo script determinista `scripts/sdd-task-state.py` (mismo principio autor≠marcador que el sellador): `init|check|next|set` con máquina de estados `PENDIENTE→EN_CURSO→HECHA|BLOQUEADA` validada mecánicamente — HECHA exige deps HECHAS, BLOQUEADA exige `--motivo`, reapertura de HECHA solo `--force`; `next` resuelve elegibilidad por Dependencies; regenera tabla `## Progreso`; compatible con `_tasks.md` legacy (init). (b) `tasks/skills/wf-task-run/SKILL.md`: default `--next` (una task por invocación, decidido con el usuario), gate de plan vigente, delegación al `Owner agent` (overlay) o implementación directa (agnóstico) con contrato de honestidad, DoD verificado con evidencia + verificación ejecutable (comandos de `<stack>_project_state.md` o runner detectado; ausencia registrada explícitamente — siembra el 2.3), commit por task `T-00X: <título> [CA-XXX]` con el `_tasks.md` incluido (decidido con el usuario). (c) gate determinista nuevo en `sdd-gate-check.py` (`gate_tasks_plan_vigente`): si el `Plan origen` del `_tasks.md` fue degradado a BORRADOR tras generar tasks → deny. (d) campo `Estado` añadido al formato canónico de `kb-tasks-expert` con regla "solo lo escribe el script". Verificado: 9 escenarios del script (transiciones válidas/ inválidas, deps, motivo, force, tabla), gate deny/allow/origen-roto, install integro en tmpdir (skills + script en `.sdd/scripts/` + rule con rootmap).*
- [x] 🔴 **2.2 — `wf-bug` / fast-lane de mantenimiento.** Un bug es divergencia entre spec y código, no un cambio de spec: flujo ligero que (a) localiza el CA violado, (b) registra el fix con trazabilidad, (c) solo escala a `wf-spec-delta` si el comportamiento esperado cambia. Sin esto el equipo abandona el pipeline al primer sprint de mantenimiento y los artefactos se vuelven mentira documental. ✅ *2026-06-07: `tasks/skills/wf-bug/SKILL.md` — triaje en 3 categorías ANTES de tocar código, con confirmación del usuario: `CODE_BUG` (código viola un CA → fix delegado por dominio de ejecución, el spec no se toca), `SPEC_CHANGE` (lo esperado difiere del CA → no se toca código, prepara el material para `/wf-spec-delta`), `UNSPEC` (ningún CA cubre el comportamiento → formalizar primero vía delta, o fix con `Aviso de gobernanza` explícito — nunca fix silencioso). Registro por feature (decidido con el usuario): `features/<n>/tasks/<n>_bugs.md` con entradas B-00X (CA, triaje, causa raíz, resolución, gobernanza), compatible con layout plano legacy. Commit `B-00X: <título> [CA-XXX]`. Verificación ejecutable como en task-run + recomendación de test de regresión. Rootmaps actualizados (tasks/CLAUDE.md con distinción task-pendiente vs divergencia-entregada, CLAUDE.md raíz, pipeline). Registry regenerado: 108 skills.*
- [x] 🟠 **2.3 — Verificación ejecutable en agentes implementadores.** *(Parcialmente sembrado por 2.1/2.2 el 2026-06-07: `wf-task-run` Paso 6 y `wf-bug` Paso 4 ya exigen build/tests con los comandos del project_state o runner detectado, y registran explícitamente cuando no hay verificación disponible. Falta: la instrucción dura DENTRO de los agentes implementadores del overlay y generalizarla al contrato de overlay.)* `kmm-feature-implementer`, `kmm-network-auth-implementer`, `kmm-platform-integrator` no compilan ni corren tests (grep de `gradle|build|run test`: solo el DoD textual del RED). → Instrucción dura de cierre: build verde + suite pasando usando los comandos del `kmm_project_state.md`, con reporte honesto si falla. Generalizar al contrato de overlay. ✅ *2026-06-07: (a) SSoT del contrato en `kb-kmm-project-state-protocol` — **Regla 6 nueva** "Contrato de cierre verificable": build + tests ejecutados de verdad con los comandos del project_state (fallback `./gradlew` estándar + señalar la laguna), resultado esperado según orden TDD (RED en `Layer: test`, verde en el resto), reporte honesto con comando y salida real (prohibido declarar "compila" sin ejecutar, prohibido maquillar fallo como éxito parcial) y ausencia declarada literal ("verificación ejecutable: no disponible — motivo"); description del frontmatter actualizada. (b) Sección corta "Cierre obligatorio: verificación ejecutable" (referencia a la Regla 6, conforme a la Regla 5 de SSoT) en los 3 implementadores **y en `kmm-tester`** (entregaba tests "listos para ejecutar" sin compilarlos — ahora un test no entregado sin ejecutar: RED en TDD, verde fuera). (c) Generalizado al contrato de overlay: `kb-sdd-stack-overlay-contract` invariante 5 + ítem de checklist de conformidad; `sdd-author` carga esa KB, así que los overlays futuros creados con `wf-stack-create` heredan la obligación sin tocar el workflow. Coherencia bidireccional declarada: la Regla 6 es la contraparte interna de lo que `wf-task-run` Paso 6 y `wf-bug` Paso 4 exigen desde fuera.*
- [x] 🟠 **2.4 — Perfil QA (`wf-qa-plan` + `wf-qa-verify`)** — trazabilidad CA → caso de prueba y verificación de cobertura de CAs tras implementar. La promesa "CAs testables GIVEN/WHEN/THEN" hoy no se cierra nunca. *(= B3 de IMPROVEMENTS)* ✅ *2026-06-07: (a) `kb-qa-expert` (SSoT de la metodología): formato TC-XXX (un TC ↔ exactamente un CA), reglas de derivación (≥1 TC por CA, TC por rama observable prometida, sin TC huérfano — comportamiento sin CA = gap de spec vía delta, mismo principio que UNSPEC de wf-bug; pureza: el TC observa comportamiento, no implementación; concreción: datos reales), niveles unit/integration/e2e/manual (manual exige justificación; el project_state del stack manda sobre la tabla genérica), proporcionalidad en modo ligero, criterios de cobertura con **regla de oro "cobertura sin evidencia ejecutada no existe"** (CUBIERTO/PARCIAL/SIN_COBERTURA/MANUAL_PENDIENTE/DIVERGENTE — estado del CA = el peor de sus TCs) y veredictos APTO/APTO_CON_RESERVAS/NO_APTO. (b) Agente `qa-engineer` (sonnet, cyan; kb-qa-expert + kb-spec-expert + kb-tasks-expert): deriva y audita, NO escribe tests ni corrige código; DIVERGENTE → `/wf-bug`, nunca se ajusta el TC para que pase. (c) `wf-qa-plan generate <spec>` → `features/<n>/tasks/<n>_qa_plan.md` (precedente de `_bugs.md`; layout plano legacy soportado) y `wf-qa-verify <qa_plan>` → localiza+ejecuta tests por TC con evidencia (comandos del project_state o runner detectado), checklist humana para manuales (CUBIERTO manual solo con confirmación explícita en sesión), escribe los `Estado` (único escritor — separación derivador/verificador) y `<n>_qa_report.md`. (d) Integración: `wf-qa-plan` en la tabla de gates deterministas (gate_spec_fiable), `wf-task-run` recomienda qa-verify al COMPLETO, rootmaps tasks+raíz, registry 114 skills. **Decisiones**: fase tasks del backbone, sin fase ni perfil de init nuevos (si surge la persona-QA como perfil, va con 5.8 multi-perfil); el QA plan puede derivarse desde que el spec es fiable (antes de implementar, para que las tasks de test sepan qué cubrir). **Bugs preexistentes corregidos al probar**: los gates y el sellador bloqueaban `status_sync: unknown` (rompía fast-track directo sin PRD; contradecía kb-traceability-rules Regla 8 — ahora bloquean solo stale/needs_review, la deriva real la caza el hash de 1.3) y la rule de tasks no se activaba con `_bugs.md` (glob ausente desde 2.2; añadido junto a los de QA). Verificado e2e en tmpdir: install (skills+agente+dependencia kb-spec-expert+globs), gate deny con [INCOMPLETO] / silencio con spec fiable unknown / deny con stale, sellador con unknown sellable y needs_review bloqueando.*
- [ ] 🟡 **2.5 — Work Unit Commits + Chained PRs** (kb de criterio de tamaño de commit, tests junto al código, división de PRs). *(= IMPROVEMENTS #4, generalizar más allá de KMM si es viable)*
- [x] 🟡 **2.6 — `wf-project-status` — informe PM read-only**: features por estado del pipeline, bloqueos, siguiente acción. *(= B2 de IMPROVEMENTS)* ✅ *2026-06-08: nuevo `wf-project-status` (fase tasks) + script determinista `sdd-project-status.py`. Por feature determina fase alcanzada (PENDIENTE/Spec/Plan/Tasks/QA/Cerrada por presencia de artefactos), estado de esa fase (spec vía `_features.md`/marcadores; plan BORRADOR/VALIDADO + enmienda; tasks X/total HECHA + bloqueadas; qa APTO/APTO_CON_RESERVAS/NO_APTO), bloqueo y siguiente acción concreta (`/wf-*`). **Read-only y mecánico, sin agente** (como `wf-sdd-status`): agrega el estado ya sellado por `sdd-seal.py`/`sdd-task-state.py`/`wf-qa-verify`/`sdd-features-index.py`, no razona ni edita; se apoya en que `_features.md` ya es índice generado fiable (5.4). Soporta layout plano legacy + features `PENDIENTE_GENERACIÓN` sin carpeta. `install.sh` lo distribuye, rootmaps (raíz + tasks) y registry (118 skills) actualizados, VERSION 0.11.0 + CHANGELOG. Verificado: 10 escenarios multi-feature (todas las fases, enmienda, bugs, spec LISTA vía índice, PENDIENTE sin carpeta, layout plano) + e2e install (script con sello + skill + ejecutable desde copia instalada).*
- [ ] 🟢 **2.7 — Noción mínima de release**: vincular el cierre de una feature a tag/SHA para que la trazabilidad llegue a producción.

---

## FASE 3 — Legacy / brownfield (la puerta al trabajo real de empresa)

Hoy el único onramp es PRD greenfield. La mayoría del software de una empresa ya existe.

- [x] 🔴 **3.1 — `wf-spec-from-code` — ingeniería inversa de specs.** Generar specs de caracterización ("esto es lo que el sistema hace hoy, verificado contra el código") como entrada alternativa al pipeline, con HUs/CAs derivados de comportamiento observable y marcados como `characterization` (no intención de negocio). Es el prerequisito de toda adopción brownfield. ✅ *2026-06-07: (a) `spec/skills/wf-spec-from-code/SKILL.md` — dos modos: `discover` (mapa de capacidades F-C-00X con evidencia obligatoria por capacidad y nota de confianza alta/media/baja; **gate humano: se detiene SIEMPRE tras el discover** para que el usuario confirme/descarte — decidido con el usuario) y `generate` (dossier de evidencia recolectado por la wf → redacción delegada a `sdd-spec-writer`; vía directa `--capability --scope` para capacidad ya nombrada por el usuario). (b) nueva `kb-spec-characterization`: regla de oro "CA sin evidencia no existe", jerarquía de evidencia (test > código con ruta > config > [INFERIDO]), header obligatorio con `Evidencia base: commit SHA`, prohibiciones (no inventar intención, no convertir bugs en deseado → `[SOSPECHA_BUG]`), degradación sin tests; cargada por `sdd-spec-writer` y `sdd-spec-auditor` (frontmatter). (c) **`[INFERIDO]` bloquea como `[INCOMPLETO]`** (decidido con el usuario), con enforcement determinista: `gate_spec_fiable` de `sdd-gate-check.py` y check 5 de `sdd-seal.py` cuentan `[INFERIDO]`; salida vía `wf-spec-gap-resolve` ampliado (confirmación humana, no analysis: confirmado/incorrecto/no-lo-sé). (d) marcadores registrados en `kb-gap-conventions` (SSoT de marcadores); rootmaps spec+raíz con excepción brownfield documentada; templates compartidos (`spec/shared/templates/` — SSoT real, redistribuida) con origen characterization. Verificado: gate deniega spec con [INFERIDO], sellador lo cuenta, install spec íntegro en tmpdir (skill + kb + frontmatters), registry 110 skills (46 wf, 64 kb).*
- [x] 🟠 **3.2 — Registro de deuda técnica en el plan.** El `plan-architect` ante incertidumbre solo puede emitir gap y detenerse. → Sección de deuda/restricciones en `_plan.md` ("lo hacemos así a pesar de X, queda pendiente Y") para que el pipeline no se bloquee ni pierda la información en legacy. ✅ *2026-06-08: (a) **frontera deuda vs gap decidida con el usuario**: deuda solo si la incertidumbre es de CÓMO (técnico, camino viable) y no de QUÉ (funcional → sigue siendo TECH_GAP y bloquea); test de admisión de 3 condiciones (camino viable + no contradice el Spec + riesgo acotado) como SSoT en `kb-plan-expert` (genérico y KMM), formato `## Deuda técnica asumida`/`### TD-00X`. (b) **Aprobación humana explícita** (decidido con el usuario): `plan-architect` propone `Aprobada por: PENDIENTE` y —a diferencia de un gap— SÍ produce el Plan; `wf-plan-validate` Paso 4b nuevo aprueba/rechaza cada TD con AskUserQuestion antes del sellado; `plan-auditor` (Check 5) degrada a TECH_GAP cualquier TD que sea gap funcional disfrazado. (c) **Enforcement**: `sdd-seal.py` condición 8 — TD incompleta o `PENDIENTE` bloquea el sellado como un gap; deuda aprobada no bloquea; sin sección, comportamiento idéntico al previo. (d) **Herencia como contexto** (decidido con el usuario): `wf-prepare-tasks`/`task-generator` (genérico + KMM) propagan `- **Deuda asumida:** TD-00X` a las tasks de los componentes afectados; `wf-task-run` la pasa al owner como restricción a respetar (no la "resuelve" por su cuenta); saldar la deuda es trabajo futuro del `Queda pendiente`, sin tasks propias. Desbloquea el brownfield de 3.1 (legacy sin tooling). Verificado: 4 casos del sellador (sin deuda / PENDIENTE / aprobada / placeholder) + e2e con install (sello 0.7.0, flujo PENDIENTE→bloquea, aprobación→VALIDADO, gate permite). Versión 0.7.0 con aviso ⚠ en CHANGELOG.*
- [ ] 🟡 **3.3 — Guía para stacks sin tooling moderno.** El contrato de overlay exige "comandos build/test"; un PHP 5 sin tests no encaja. → Degradación documentada: project_state con `tests: none`, estrategia de characterization tests primero, criterios de plan para monolitos sin módulos.
- [ ] 🟡 **3.4 — `wf-design-extract` — ingeniería inversa de UI existente.** `wf-design-system` solo sabe "leer DESIGN.md" o "crear desde cero"; falta derivar el `DESIGN.md` de la UI ya en producción (CSS/capturas/componentes) para proyectos que no se van a rediseñar.

---

## FASE 4 — Scrum cambiante: coste de sincronización y modo ligero

El camino feliz son ~13 invocaciones + 4-5 checkpoints humanos por feature. En sprints de 2 semanas, el overhead mata la adopción.

- [x] 🟠 **4.1 — Modo ligero declarado del pipeline.** Camino spec→plan→tasks con discovery/moodboard/design opcionales por feature, para features pequeñas y proyectos sin UI. Hoy hasta el `fast-track` exige los 8 elementos y ≥3 CAs. Definir explícitamente qué gates son irrenunciables y cuáles son proporcionales al tamaño de la feature. ✅ *2026-06-07: principio implementado = **proporcionalidad declarada, no gates relajados**. (a) SSoT nueva en `kb-spec-expert` ("Modo ligero"): tabla de invariantes que NO cambian (CAs GIVEN/WHEN/THEN testables, trazabilidad+status_sync, marcadores bloquean, pureza, gobernanza de alcance — los gates deterministas no distinguen modos y no se tocaron) vs tabla de lo proporcional (≥3 CAs → ≥1; 8 elementos → núcleo de 4: Actores/HUs/CAs/Fuera de Alcance, resto `N/A — modo ligero` EXPLÍCITO, nunca rellenado por inventar; analysis inline; design solo con UI reutilizando brief/DESIGN.md de producto — sin re-intake por feature; conflict/readiness opcionales sin shared models; changelog mínimo). (b) Declaración en dos niveles (decidido con el usuario): `pipeline_mode: standard|light` en `project-init.json` (pregunta 5.8 nueva del init, default standard, reflejado en resumen 5.9 y CLAUDE.md generado) + override por feature `--light`/`--standard` en `wf-spec-fast-track` (precedencia: flag > project > standard) y propagado por `wf-spec-features-first` a sus subagentes; el spec ligero lleva `> Modo: ligero` en el header para que las fases posteriores lean el modo del artefacto, sin re-flags. (c) `wf-spec-validate` valida por modo: ligero = núcleo de 4 + N/A explícitos (sección ausente sin marca = hallazgo); pureza y testabilidad idénticas. (d) Sin entrypoint compacto encadenado (decidido con el usuario: eso es el 4.4, para diseñarlo junto a la cadena de re-sync). Rootmaps spec+raíz actualizados con los flags.*
- [x] 🟠 **4.2 — Back-edge tasks→spec.** Cuando el dev descubre implementando que un CA es ambiguo, hoy el coste es re-descender 4 fases. → Flujo corto de "enmienda desde implementación": corrige el CA, marca los artefactos intermedios afectados como stale puntual, sin re-correr todo el waterfall. ✅ *2026-06-07: (a) nueva `wf-spec-amend <spec> --ca CA-XXX [--from-task T-00X]` (fase spec, agente sdd-spec-writer): gate de clasificación estricto — solo ACLARACIÓN (la intención GIVEN/WHEN/THEN no cambia; caso dudoso = CAMBIO → escala a `wf-spec-delta`, decidido con el usuario), confirmación humana del texto, edición quirúrgica con versión menor + changelog `E-00X: aclaración CA-XXX desde T-00X`, y revisión scoped de las secciones del plan que referencian el CA (conservadora: en duda, no limpia). (b) **Stale puntual** (anotación + bloqueo selectivo, decidido con el usuario): nuevo script `sdd-amend.py` (mark/clear/list/next-ref) — único escritor de `> **Enmienda pendiente:** CA-XXX (E-00X, fecha)` en el header del plan, con numeración E-NNN determinista (escanea plan + changelog del spec); el plan conserva VALIDADO. (c) Retención determinista: `sdd-gate-check.py` deniega `wf-task-run --task` sobre tasks cuyo `Spec CA` toca un CA enmendado y `wf-prepare-tasks` sobre planes con enmiendas abiertas; `sdd-task-state.py` `next` salta retenidas (`RETENIDAS_POR_ENMIENDA`), `check` las marca, `set EN_CURSO` las rechaza salvo `--force`; `sdd-seal.py --seal` absorbe las anotaciones al re-validar (`--check` no las toca). (d) `wf-task-run` integra la salida (owner reporta CA ambiguo sin elegir interpretación; bloqueo → BLOQUEADA + `/wf-spec-amend`); semántica = Regla 9 nueva de `kb-traceability-rules`; rootmaps raíz/spec/tasks + README spec; `install.sh` distribuye el script. Verificado: 11 casos del script + roundtrip byte-idéntico mark→clear/mark→seal + 10 escenarios de retención (gate deny selectivo, next saltando, set rechazado, absorción por sellado) + e2e con install en tmpdir (script estampado 0.6.0, skill instalada, flujo completo desde copias instaladas). Versión 0.6.0 con aviso ⚠ en CHANGELOG.*
- [x] 🟡 **4.3 — `wf-design-sync` o equivalente.** Tras un cambio de spec/`DESIGN.md`, nada detecta qué `_views.md`/tokens quedaron stale (el "considerar regenerar" de `wf-design-delta` es manual). Réplica del patrón `wf-prd-sync-impact` para la fase design. ✅ *2026-06-08: nuevo `wf-design-sync <DESIGN.md>` (fase design, delega a `design-architect`), espejo de `wf-prd-sync-impact`. Descubre los derivados (DESIGN_BRIEF, `_flows`/`_views`/`_ui_prompt` por feature —subcarpeta o plano legacy—, exports de tokens, specs fuente) y asigna `in_sync`/`needs_review`/`stale`/`unknown` por artefacto según el grafo de dependencias de la fase (brief→DESIGN.md→views/ui_prompt/exports; spec→flows/views/ui_prompt), con criterio conservador (`kb-design-governance`/`kb-design-expert`). Produce `_design_sync_report.md` con matriz + acción recomendada (`wf-design-delta`/`wf-design-feature-prototype`/`wf-design-export`); read-only sobre los derivados. **Alcance**: análisis de impacto, no sellado por hash — el sellado determinista de deriva en Design (à la `sdd-sync-check.py`) queda anotado como candidato en 11.2. Rootmaps (raíz + design) + tabla de precondiciones actualizados; registry 119 (+1 wf), VERSION 0.14.0 + CHANGELOG. Sin scripts nuevos.*
- [x] 🟡 **4.4 — Compactar la cadena de re-sync.** Un cambio de producto dispara ~9 workflows encadenados a mano. → Orquestar la cadena completa con un solo comando (`wf-prd-change --cascade`) que pare solo en los checkpoints humanos reales. ✅ *2026-06-09: nuevo `wf-prd-change-cascade <prd.md> --new-reqs <cambio.md>` (fase prd — donde nace el disparador). **Decisión de arquitectura (con el usuario): skill orquestador nuevo, NO flag en `wf-prd-change`** — `wf-prd-change` es prd-phase + `agent:prd-expert`; meter orquestación cross-fase dentro violaría SRP/capas. El cascade es un orquestador puro (mismo patrón que `wf-spec-features-first`: sin campo `agent:`, no analiza ni escribe contenido, invoca los workflows existentes en orden, `allowed-tools: [Read, Write, Bash, Skill]`). Encadena `wf-prd-change` → `wf-prd-sync-impact` → `wf-spec-sync-from-prd analyze` → `apply` → `wf-spec-conflict` + `wf-spec-readiness` → `wf-design-sync` → reporte de plan/tasks stale, y produce `<basename>_cascade_report.md`. **Autonomía (con el usuario): mecánico auto + parar en gates reales** — corre el pegamento read-only Y auto-aplica los deltas de spec inequívocos (`severidad: minor` + `acción: delta`); para solo en los 4 checkpoints humanos reales: (1) aprobación del cambio (la materializa `wf-prd-change`), (2) features con cambio no trivial (`major`/`manual_review`/`rediscover` — se listan sin aplicar, sin abortar el cascade), (3) decisiones visuales de Design (`wf-design-sync` solo diagnostica), (4) revalidación de plan + deuda. Flags: `--review-before-apply` (restaura parada conservadora antes de cualquier escritura), `--dry-run` (solo diagnostica), `--features` (acota subset), `--skip-design`. **Profundidad adaptativa** (solo entra en Design si hay `DESIGN.md`, solo reporta plan/tasks si existen, resuelve `phases`/`artifacts` de `project-init.json`) + **degradación con gracia** (sub-workflow no instalado → `no disponible` sin abortar). Creado vía `/wf-skill-create` → `sdd-author` (+ refactor de autonomía delegado al mismo autor). Rootmaps (raíz + prd) y nota de pipeline actualizados; `install.sh prd` lo recoge por glob (sin tocar el installer); registry 120 skills (+1 wf); VERSION 0.15.0 + CHANGELOG con ⚠. Sin scripts nuevos. Pendiente de validación en uso real: forma parte del barrido de la Fase 11 (11.1 paso de args en la cadena orquestador→skill, 11.6 playbook de cambio de producto).*

---

## FASE 5 — Multi-desarrollador, distribución y CI

Diseñado hoy para un operador omnisciente en una máquina. Una empresa son N devs, N repos, CI.

- [x] 🔴 **5.1 — Versionado del ecosistema + update no destructivo.** No existe `VERSION` en ninguna parte y `wf-project-init` prohíbe sellar versión. → `VERSION`/commit sellado como `sdd_version` en `project-init.json` + `wf-project-update` que reinstale fases respetando overlays, sin re-entrevistar. *(= C1 de IMPROVEMENTS)* ✅ *2026-06-07: (a) `sdd/VERSION` (semver, arranca en 0.1.0) + `sdd/CHANGELOG.md` (entradas por versión; líneas `⚠` = cambios que afectan a proyectos ya inicializados). (b) `install.sh` sella `.sdd/sdd-version.json` ({version, commit, installed_at, installed_by}) y estampa `# sdd-version: X+sha` en línea 2 de cada script de enforcement copiado (viajan commiteados a CI sin el ecosistema al lado); el overlay KMM refresca el sello. (c) `bootstrap/skills/wf-sdd-update/` (distribuida global por `setup.sh` + symlink dev, junto a wf-project-init): relee fases/stack de `project-init.json` SIN re-entrevistar, muestra plan con avisos `⚠` del CHANGELOG entre versiones, ejecuta `install.sh <fases> --prune` y DESPUÉS el overlay (orden que preserva las sustituciones por basename — absorbe el flujo correcto del 6.6), sincroniza `sdd_version` en project-init.json, verifica rules+sello+stamp; no toca artefactos ni `<stack>_project_state.md`; fallback para instalaciones standalone sin init (deriva fases de las rules). (d) hook de sesión: directiva nueva `[SDD-PROTOCOL] version-drift` — informativa, nunca bloquea, solo si ambos sellos son legibles y difieren; protocolo documentado en `claude-global-block.md`. (e) `wf-project-init` schema con `sdd_version` (prohibición de versión eliminada) + check de sello en Paso 9; `wf-sdd-status` reporta versión y deriva. Verificado: install sella y estampa (script sigue ejecutable), hook calla con sello igual / avisa con sello viejo / vuelve a callar tras simular el ciclo update completo (reinstall --prune + sync de project-init.json); bash -n en los 4 shell scripts; registry 111 skills. ✓ Resuelto 2026-06-07: `setup.sh --update` ejecutado; hook y bloque global de ~/.claude verificados en sync.*
- [x] 🟠 **5.2 — Modo CI/headless.** Todo el arranque depende de `AskUserQuestion` bloqueante. → Variable de entorno o flag (`SDD_NON_INTERACTIVE=1`) que suprima el protocolo de sesión, y documentar `.claude/sdd-mode.json` commiteado como opt-out válido para runners. ✅ *2026-06-07: el hook de sesión sale silencioso (exit 0, ninguna directiva) con `SDD_NON_INTERACTIVE=1` (opt-out explícito) o `CI=true` (estándar de facto de los runners — GitHub Actions, GitLab, CircleCI); chequeo al principio del hook, antes de cualquier estado. Sección nueva "Modo CI/headless" en `claude-global-block.md`: documenta las variables, instruye no aplicar tampoco el fallback manual bajo ellas, y declara `.claude/sdd-mode.json` commiteado como el opt-out por repo (viaja en git, sin variables). Los gates/sellador ya funcionaban headless (commiteados en `.sdd/scripts/` con sello — 5.1). Verificado: matriz de 5 casos (virgen sin vars → directiva; SDD_NON_INTERACTIVE=1 → silencio; CI=true → silencio; CI=false → directiva; proyecto con deriva de versión + CI=true → ni el aviso informativo). ✓ Resuelto 2026-06-07: instalación global re-sincronizada con `setup.sh --update`.*
- [x] 🟠 **5.3 — Política documentada de qué se commitea.** Qué va a git de `.claude/` y `.sdd/` (artefactos sí, skills copiadas ¿sí/no?, `settings.local.json` nunca), plantilla de `.gitignore` que `wf-project-init` deje en el proyecto, y guía de onboarding del dev nuevo (clonar ecosistema + `setup.sh` + `~/.sdd-home`). ✅ *2026-06-07: **política decidida: se commitea TODO** `.claude/` (skills, agents, rules, settings.json) y TODO `.sdd/` (estado, scripts de enforcement, sello) — el repo funciona para cualquier dev y en CI sin el ecosistema instalado, que era el criterio decisivo (las skills copiadas cambian solo en updates, el ruido de merge es mínimo y 5.1 da la vía de actualización uniforme); única excepción `.claude/settings.local.json`. Aplicado en: (a) plantilla del CLAUDE.md raíz generado — sección "Política de git"; (b) `wf-project-init` Paso 7 asegura la línea en `.gitignore` (crea el archivo si no existe, no toca el resto) + check en Paso 9; (c) `wf-sdd-update` Paso 5b la asegura también (migración de proyectos pre-0.5.0); (d) README del ecosistema — sección "Onboarding y política de git": tabla de qué se commitea, flujo del dev nuevo (clonar proyecto → funciona ya; ecosistema+setup.sh recomendado para protocolo/updates/inits) y guía CI (conecta con 5.2); de paso reescrita la sección "Instalación" del README, que aún describía el modelo antiguo de copiar a `~/.claude/` global.*
- [x] 🟠 **5.4 — Estrategia de concurrencia para artefactos hub.** `_features.md` y `stack_workflows_run[]` garantizan conflicto de merge con 2 devs en paralelo. → Formato append-only o por-feature (un fichero por feature en vez de hub monolítico) + guía "una feature por rama". ✅ *2026-06-08 (decidido con el usuario: índice regenerable por script, la opción más coherente con el precedente `generate-skill-registry.py`; cierra de paso el candidato `_features.md` de 11.2). (a) **`_features.md` pasa a artefacto GENERADO** por el script determinista nuevo `sdd-features-index.py`: ya no es un hub monolítico editado a mano (que los fast-tracks pisaban en paralelo y dos devs en ramas distintas colisionaban en cada merge). Salida = función pura de las fuentes (SIN timestamp de reloj) → idempotente: regenerar tras un merge da bytes idénticos, un conflicto sobre el hub es ruido. Modos regenerar / `--check` (exit 2 si desactualizado, para CI) / `--stdout`; escritura atómica (tempfile + `os.replace`) → seguro ante regeneraciones concurrentes. (b) **SSoT repartido y co-localizado** (ninguna fuente redefine lo de otra): `_discovery.md` = universo + shared models + RF→Feature; cada `spec` = feature generada + marcadores reales (`[INCOMPLETO]`/`[CRÍTICO]`/`[INFERIDO]`) + Feature ID + HUs/CAs; `_readiness_report.md` (`## Matriz de readiness`) = veredicto autoritativo del estado. El script agrega estructura, no prosa libre. Estado por prioridad: sin spec→PENDIENTE_GENERACIÓN; con spec→veredicto readiness si existe, si no marcador-based (marcador→BLOQUEADA, gobernanza≠ninguno→REQUIERE_CAMBIO_PRD, limpio→LISTA). (c) **5 productores reconectados** (ya no hand-escriben; llaman al script): `wf-spec-fast-track`, `wf-spec-features-first` (Paso 6 = pasada autoritativa única tras los fast-tracks), `wf-spec-readiness` (regenera desde su report — realinea con su propia declaración "no modifica artefactos"), `wf-spec-delta` (Paso 6B.5), `wf-spec-from-code`. 4 guías de referencia huérfanas eliminadas; `kb-decompose-expert` documenta el formato generado (sin `## Historial de cambios` manual — el audit-trail vive en el changelog de cada spec + git); DIAGRAMS actualizado. (d) **`stack_workflows_run` → log append-only `.sdd/stack-runs.jsonl`** (una línea JSON por run, escrito con `>>` por `wf-<stack>-init`): sale del array en `project-init.json` (config compartida = conflicto garantizado), que queda como config estable. Schema de `wf-project-init`, `wf-kmm-init` Paso 7, scaffold de `wf-stack-create` y `kb-sdd-stack-overlay-contract` actualizados. (e) Guía **"una feature por rama"** + `_features.md` generado en el CLAUDE.md raíz de `wf-project-init` y el README; `install.sh` distribuye el script; VERSION 0.9.0 + CHANGELOG con ⚠. Verificado: 7 escenarios del script (estados, idempotencia byte-idéntica, `--check` 0/2, readiness autoritativo, layout plano legacy, sin discovery, dir vacío); `from __future__ import annotations` para compat py3.9 macOS.*
- [ ] 🟡 **5.5 — Roles y aprobaciones diferenciadas.** Todos los checkpoints son "el humano valida" (singular). → Mínimo viable: campo `approved_by: <rol>` en los sellados (PM en PRD, tech lead en plan, QA en verify) para que la autoría de cada gate quede trazada.
- [ ] 🟡 **5.6 — Opt-out/desinstalación del bootstrap global.** No existe `setup.sh --uninstall`; el hook dispara el wizard en todos los proyectos del dev. → Uninstall limpio + allowlist/denylist de rutas en el hook.
- [ ] 🟡 **5.7 — Soporte de monorepo en el hook y el init.** Abrir en raíz vs subpaquete da estados SDD distintos. Relacionado con A3 (multi-stack); como mínimo, que el hook busque `.sdd/` hacia arriba hasta la raíz git.
- [ ] 🟢 **5.8 — Multi-perfil acumulado** — `profiles: [...]` en `project-init.json` (hoy solo guarda el último). *(= C2 de IMPROVEMENTS)*

---

## FASE 6 — Multi-stack real

Solo existe KMM; los overlays son mutuamente excluyentes por diseño; el proyecto políglota (backend+front+móvil) no está soportado.

- [ ] 🟠 **6.1 — Resolver la exclusividad mutua de overlays.** El override por `basename` (`plan-architect.md`…) hace imposible dos stacks en un repo. → Diseñar resolución por ruta/módulo (`stacks: [{path, stack}]` en `project-init.json`, gates resolviendo stack por ubicación de la feature). *(= A3 de IMPROVEMENTS — esperar a 2-3 overlays reales, pero diseñar el contrato ya para no cavar más hondo)* 🔶 *2026-06-08: **slice de DISEÑO/contrato hecho** (decidido con el usuario: diseñar el contrato ya, implementación cuando exista el 2º overlay). `kb-sdd-stack-overlay-contract` extendida con: (1) sección "Modelo objetivo: multi-stack por ubicación" — `"stack": "<único>"`+`specialist_workflow` declarado forma transitoria/azúcar de un `stacks: [{path, stack, specialist_workflow}]` de un elemento; migración aditiva (single-stack ≡ `stacks: [{path: ".", ...}]`); (2) regla de resolución por ubicación de la feature en los gates de `wf-prepare-plan`/`wf-prepare-tasks` (exigen el `<stack>_project_state.md` del stack de ESA ruta; feature fuera de toda entrada → genérico); (3) exclusividad mutua declarada limitación transitoria conocida (no invariante) + directiva "ninguna pieza nueva profundiza el supuesto de stack único" + ítem en el checklist de conformidad; (4) nota de que la resolución vive hoy en prosa por campo global y el objetivo la lleva al gate determinista parametrizado por ruta; subsección "Qué NO incluye este diseño" (no especifica el algoritmo del resolver, no cambia el basename, no toca install.sh/schema/gates). **Implementación PENDIENTE** hasta 6.5 (2º overlay real que haga el conflicto reproducible y testeable). Puntos de aterrizaje anotados para entonces: `wf-prepare-plan`/`wf-prepare-tasks` (resolución hoy por campo global ~L36-41), `wf-project-init` schema (campo `stacks[]`, ~L442-457), `kb-sdd-skill-architecture` Regla 16 y `wf-stack-create` (heredar la regla "no cavar más hondo").*
- [x] 🟠 **6.2 — Extraer el núcleo metodológico compartido de los agentes override.** El cuerpo de `plan-architect`/`plan-auditor`/`task-generator` está copiado a mano entre genérico y KMM; con 5 stacks, cada cambio metodológico = 6 ediciones manuales. → Mover el contrato común a una KB compartida que ambas variantes carguen. ✅ *2026-06-08: medido primero (exploración: ~72% duplicado en plan-architect, ~75% en plan-auditor, ~48% en task-generator; ~280 líneas copiadas dos veces). **Decidido con el usuario: 2 KBs por fase** (no por rol ni una sola) y **alcance solo cuerpos de agente** (kb-plan-expert/kb-tasks-expert, que también tienen variante genérica+KMM, quedan para un ítem aparte). (a) Creadas `kb-plan-method` (Sección A producir / Sección B auditar) y `kb-tasks-method` como SSoT del *cómo operar*, stack-agnósticas, con hooks `‹especialización de stack›` en los puntos que varían (nomenclatura de capas, topología técnica, mecanismo de plataforma, owners, templates). (b) Los 6 agentes adelgazados: cargan la method KB en `skills:` + solo su especialización (genérico "el repo manda"; KMM Clean Architecture/Koin/expect-actual/Compose/AppResult). (c) Las method KB no se sobreescriben (sin variante de stack); viven en `plan/skills/`+`tasks/skills/`, la base las instala y ambas variantes las cargan por nombre (mismo patrón cross-fase que kb-a11y-expert). `install.sh` las recoge por glob sin tocar dependencias. (d) `kb-sdd-stack-overlay-contract`: regla nueva + ítem de checklist — los overlays futuros cargan la method KB en vez de re-copiar metodología (conecta con 6.1). Verificado e2e: install genérico y overlay KMM, method KBs presentes tras el overlay, los 3 agentes con sus 10/7/5 KBs resolviendo, especialización KMM conservada, kb-plan-expert KMM aún sustituye al genérico. Registry 117 skills. Versión 0.8.0, refactor sin cambio de comportamiento (gates y sellador intactos).*
- [x] 🟡 **6.3 — Adaptación al repo destino en los implementadores.** La variante KMM de `plan-architect` eliminó la instrucción genérica "fundamenta toda decisión en la exploración real del repo" y la sustituyó por dogma KMM (Koin, AppResult…). Un repo KMM real con otras convenciones recibirá imposiciones. → Restaurar la regla de "el repo manda sobre el dogma" en todos los overlays. ✅ *2026-06-08: hallazgo al explorar — tras 6.2 la regla NO estaba simplemente borrada; `kb-plan-method` codificaba un contraste "genérico = repo manda / overlay = dogma del stack manda", que ERA la degradación. Fix **sistémico en 3 capas** (no parche KMM): (a) **`kb-plan-method` + `kb-tasks-method`** (hook "Overlay de stack") reformulados — el canon del stack es **default para greenfield/silencio del repo**, pero **el repo destino manda cuando diverge** (convenciones reales en `<stack>_project_state.md`); el canon rellena huecos, no sobrescribe lo presente. Cubre `plan-architect`/`plan-auditor`/`task-generator` (genérico + KMM, todos cargan la method KB). (b) **`kb-kmm-project-state-protocol` Regla 7** nueva: el project_state (repo real) manda sobre el canon KMM (Hilt vs Koin, Result vs AppResult, módulos, R.* en target único); aplica a TODOS los agentes KMM incl. los **implementadores** (que cargan esta KB, no el method) — esa era la parte "en los implementadores" del ítem. (c) **`kb-sdd-stack-overlay-contract` invariante 6** + checklist: "el repo manda sobre el dogma" para todo overlay futuro (lo heredan los creados con `wf-stack-create`). Puntero corto de precedencia añadido al cuerpo del `plan-architect` KMM (el que el ítem señaló). Refactor de criterio, gates/sellador intactos; registry 118 (una description), VERSION 0.13.0 + CHANGELOG con ⚠.*
- [ ] 🟡 **6.4 — Completar huecos del propio stack KMM** (de `tech/kmm/BACKLOG.md`): `kb-kmm-gradle-modules`, persistencia (Room), `kb-kmm-offline-strategy`, `kb-kmm-secrets-cicd`.
- [ ] 🟢 **6.5 — Primer overlay adicional con `wf-stack-create`** (orden sugerido: `android`, `flutter`, `ios`, `next`, `fastapi`/`ktor`) — servirá además de test real del contrato de overlay y de 6.1/6.2.
- [x] 🟠 **6.6 — Reinstalar fases genéricas pisa los overrides del overlay sin avisar.** *(Descubierto durante la Fase 0, 2026-06-05.)* `install.sh` hace `rm -rf`+`cp` por nombre: re-ejecutarlo con `plan`/`tasks` sobre un proyecto con overlay KMM sustituye `kb-plan-expert`/`kb-tasks-expert`/`plan-architect.md`/etc. (variante stack) por la genérica, degradando el proyecto en silencio. → El installer debería leer `stack` de `.sdd/project-init.json` y, si hay overlay, re-aplicarlo al final (o avisar de que hay que re-ejecutar `tech/<stack>/install.sh`). *(Mitigado parcialmente por 5.1 el 2026-06-07: la vía soportada de reinstalación es ahora `/wf-sdd-update`, que siempre re-aplica el overlay después de la base.)* ✅ *2026-06-07: paso 7 nuevo en `install.sh` — lee `stack` de `$ENFORCE_ROOT/.sdd/project-init.json` (python3, validación del valor, conservador: JSON malformado o sin python3 → silencio) y, si este run instaló plan/tasks (donde viven los overrides): overlay existente → re-ejecuta `tech/<stack>/install.sh` desde la raíz del proyecto restaurando las variantes; overlay inexistente en el ecosistema → aviso explícito de degradación en vez de callar. Mismo orden base→overlay que `wf-sdd-update`. Verificado e2e en tmpdir: overlay KMM instalado → `install.sh plan,tasks` a mano → variante KMM de `plan-architect` intacta (3 menciones KMM, model correcto); stack `flutter` sin overlay → aviso; proyecto sin stack → sin ruido.*

---

## FASE 7 — Fase Design: declarar el fit y cerrar grietas

Su encaje natural hoy es greenfield mobile sin diseñador con Stitch como destino — pero no está declarado, y genera falsas expectativas.

- [x] 🟠 **7.1 — Declarar el fit explícitamente** en `design/README.md` y `design/CLAUDE.md`: para quién es (equipos sin diseñador / greenfield), qué no cubre (integración Figma, reverse de UI existente), y que es saltable por feature sin UI (hoy esa señal solo vive en `plan/`; propagarla a `sdd/CLAUDE.md` y a la propia fase). ✅ *2026-06-07: (a) `design/CLAUDE.md` — sección nueva "Fit de esta fase": para quién ES (sin diseñador / greenfield / mobile-first con Stitch), qué NO cubre hoy (Figma, reverse de UI existente, equipos con diseñador propio — con la vía de escape documentada: DESIGN.md a mano respetando `kb-design-system-contract` + `wf-design-validate`), saltable por proyecto y por feature, e instrucción al orquestador de decirlo en cuanto detecte un caso "NO cubre" — antes de generar artefactos inútiles; (b) `design/README.md` — tabla "Fit declarado" equivalente para el lector humano; (c) `sdd/CLAUDE.md` — nota en el pipeline: Design saltable por feature sin UI, regla canónica en `kb-plan-expert` aplicada por `wf-prepare-plan` (la señal ya no vive solo en plan/). La decisión formal sobre Figma queda en 7.6 (el fit dice "no cubre HOY", sin cerrar la puerta).*
- [ ] 🟡 **7.2 — Anclar la dirección visual cuando no hay brief.** Con `--no-brief` + research pobre, `style_family`/paleta/motion son invención plausible con formato de autoridad. → Exigir confirmación humana explícita de la dirección visual en modo `auto`, o marcar el `DESIGN.md` resultante como `provisional` hasta validación.
- [ ] 🟡 **7.3 — Variante web de a11y y prototipado no-Stitch** (WCAG completo, targets no-táctiles). *(= B4 de IMPROVEMENTS)*
- [ ] 🟡 **7.4 — Resolver solapamiento `kb-design-style-decision-tree` vs `kb-design-style-taxonomy`.** El árbol re-deriva los mismos criterios (B2B→high density…) que la taxonomy define — divergencia futura asegurada. → Una de las dos es la SSoT; la otra referencia.
- [ ] 🟢 **7.5 — Evaluar consolidación de workflows pre-DESIGN.md** (`intake`+`discover`+`moodboard`) y la pareja `branch`/`variant` para equipos pequeños — la gobernanza ya necesita un decision-tree solo para explicar la diferencia, síntoma de sobre-segmentación.
- [ ] 🟢 **7.6 — Integración Figma (evaluar).** Import de tokens/variables y export Figma Tokens en `wf-design-export`. Solo si el fit declarado en 7.1 decide cubrir equipos con diseñadores; si no, descartar explícitamente y documentarlo.

---

## FASE 8 — Persistencia y memoria entre sesiones *(de GENTLE_AI_COMPARISON)*

- [ ] 🟡 **8.1 — State Recovery post-compactación.** Estado del DAG por feature persistido en artefacto (`phase`, `completed[]`, `pending[]`) para que el orquestador se reoriente tras compactar sin re-preguntar al usuario. El pipeline de 13 pasos es exactamente donde la sesión se compacta.
- [ ] 🟡 **8.2 — Configuración de proyecto persistente** (tipo `config.yaml`): "TDD estricto", comandos de test, reglas — leída en el init y propagada a agentes. Parcialmente cubierto por `kmm_project_state.md`; generalizar al nivel de proyecto.
- [ ] 🟢 **8.3 — Session Close Protocol** formalizado (Goal / Instructions / Discoveries / Accomplished / Next Steps / Relevant Files) al cierre de sesión.
- [ ] 🟢 **8.4 — Proactive Save Triggers** — lista normativa de cuándo guardar en memoria sin esperar al usuario (decisión arquitectónica, gotcha, convención establecida…).

---

## FASE 9 — Higiene de frontmatter y meta *(restos de audit_full_global, estado re-verificado 2026-06-05)*

- [ ] 🟡 **9.1 — `argument-hint` en 49 `kb-*`** (re-verificado: subió de 47). El template canónico lo prohíbe en KBs. → Limpieza masiva por lotes con `/wf-sdd-refactor`, o actualizar el template si se decide mantenerlo — pero decidir una política y aplicarla. El meta-ecosistema incumple su propio canon en ~90% de sus KBs.
- [ ] 🟡 **9.2 — `wf-prd-create`: falta `Agent` en `allowed-tools`** (re-verificado: sigue `[Read, Write, Bash]` con `agent: prd-expert`).
- [ ] 🟡 **9.3 — `agent: prd-expert` fantasma en `wf-prd-change` y `wf-prd-review`** (re-verificado: sigue presente; los cuerpos operan inline). → Eliminar el campo o delegar de verdad.
- [ ] 🟢 **9.4 — `when_to_use` en 4 `kb-*` restantes** (re-verificado: bajó de 37 a 4). Rematar la limpieza.
- [ ] 🟢 **9.5 — `plan-auditor` no carga `kb-plan-cmp-ui`/`kb-cmp-resources`** que sí carga `plan-architect` — evaluar si el auditor debe verificar decisiones CMP.
- [ ] 🟢 **9.6 — Documentar en `wf-design-intake` que es autónoma** (no delega a agente) para que la tabla de capas del CLAUDE.md de design no confunda.
- [ ] 🟢 **9.7 — Cognitive Doc Design** — principios de calidad cognitiva en `kb-sdd-creation-guide` (respuesta primero, tablas > prosa, referencias > repetición). *(= IMPROVEMENTS #5)*
- [ ] 🟢 **9.8 — Enforcement del ciclo de calidad meta:** la detección de duplicados de `wf-skill-create` es por substring del nombre (frágil); el audit estructural es manual. → Evaluar pre-commit/CI que corra el chequeo estructural básico.
- [x] 🟡 **9.10 — Actualizar el modelo pineado de los agentes a `claude-opus-4-8`.** *(Pedido por el usuario 2026-06-07.)* Estado real verificado por grep: 8 agentes + el canon de `kb-sdd-creation-guide` (SKILL.md, `references/frontmatter-templates.md`, `references/checklists.md`) pinean `claude-opus-4-7` (no 4-6 — esa ya se limpió en Fase 0): `design-architect`, `plan-architect` (genérico y overlay KMM), `sdd-author`, `prd-expert`, `kmm-network-auth-implementer`, `kmm-feature-implementer`. → Cambiar a `claude-opus-4-8` empezando por el canon (la SSoT de qué modelo asignar por tipo de pieza) y después los 8 agentes; evaluar de paso si los 13 `claude-sonnet-4-6` siguen siendo la asignación correcta por rol o conviene revisión. ✅ *2026-06-07: canon actualizado (SKILL.md ×3 menciones, frontmatter-templates, checklists) + los 7 agent files con pin opus (el "8º" del conteo era la mención obsoleta `claude-opus-4-6` de `README.md:467`, también corregida). Verificado por grep: cero `claude-opus-4-7`/`4-6` fuera de ROADMAP/CHANGELOG; distribución final 7×opus-4-8 + 13×sonnet-4-6. Evaluación sonnet: se mantienen — `claude-sonnet-4-6` es la versión Sonnet vigente y sus roles (escritura estructurada, exploración, auditoría, planificación) coinciden con la tabla de criterios del canon.*
- [ ] 🟡 **9.9 — Presupuesto de tokens para `description` de kb-\*.** *(Derivado del afinado de 10.3, 2026-06-05.)* Las descripciones de TODAS las skills (incluidas las ~60 kb-*) entran eager en la lista del orquestador en cada sesión — es el único coste fijo que escala con el inventario, y las kb no son punto de entrada del orquestador. No se pueden sacar de `.claude/skills/` (el harness resuelve desde ahí la inyección a agentes), pero sí acortar: → fijar presupuesto (p. ej. ≤25 palabras) para `description` de kb-* en `kb-sdd-creation-guide` y pasar `wf-sdd-refactor` por las que lo excedan. Las wf-* conservan descriptions ricas (su `when_to_use` ES el routing).

---

## FASE 10 — Estructura y topología de proyectos *(añadida 2026-06-05 tras prueba real)*

Hoy las skills resuelven rutas de salida "relativas al archivo de entrada" y todo el sistema asume artefactos + código en el mismo repo. Ninguna de las dos cosas aguanta un proyecto real.

- [x] 🟠 **10.1 — Contrato de ubicación de artefactos por fase.** Las skills escriben junto al archivo de entrada: `wf-spec-analyze/SKILL.md:129` ("mismo directorio que el archivo de entrada"), `wf-spec-fast-track/SKILL.md:145` ("relativos al directorio del archivo de entrada"). **Caso real (2026-06-05):** proyecto con carpeta `specs/` → los artefactos de spec se crearon en la carpeta del `PRD.md`. No existe layout configurable. → Añadir mapa `artifacts:` a `.sdd/project-init.json`, que `wf-project-init` rellene **detectando carpetas existentes** del proyecto (y preguntando si hay ambigüedad); todas las wf-* resuelven sus rutas de salida desde ese mapa (SSoT de layout), con la convención actual como default si no está configurado. ✅ *2026-06-05: (a) schema de `project-init.json` con mapa `artifacts` ({prd, spec, design}; plan/tasks viven siempre junto al spec de su feature para preservar trazabilidad relativa); (b) `wf-project-init`: detección de carpetas candidatas (3d: specs/, docs/specs, docs/prd…), pregunta 5.7 solo si hay candidatas, layout documentado en el CLAUDE.md raíz generado (tabla "Layout de artefactos") y checks en Paso 9; (c) regla de layout aplicada en 6 workflows de escritura (spec-analyze, spec-discover, spec-fast-track, prd-create, design-intake, design-system) + búsquedas de analysis/discovery en features-first y los patrones de los CLAUDE.md de spec y global — siempre con el comportamiento actual como fallback; (d) **fix de bug derivado de 1.1/1.2**: wf-project-init instala fases con cwd en subdirectorio, así que los scripts de enforcement acababan en `<fase>/.sdd/` — añadido `SDD_PROJECT_ROOT` a `install.sh` y paso explícito en wf-project-init que copia scripts y mergea settings en la RAÍZ del proyecto. Verificado: install por fase con SDD_PROJECT_ROOT → scripts en raíz sin basura en subdirs; uso standalone intacto.*
- [x] 🟠 **10.3 — Infraestructura única: un solo `.claude/` raíz, sin carpetas-cáscara de fase.** *(Decidido con el usuario 2026-06-05.)* → Instalar TODAS las skills/agents en el `.claude/` raíz; CLAUDE.md de fase como docs lazy en `.claude/phases/<fase>.md`; sesiones siempre en raíz. ✅ *2026-06-05: `install.sh` instala docs de fase en `phases/` y mantiene CLAUDE.md raíz (que el Paso 7 del init siempre sustituye por la plantilla del proyecto); el overlay KMM ya NO pisa el CLAUDE.md raíz (iba directo a `.claude/CLAUDE.md` — bug preexistente; ahora va a `phases/kmm.md`); `wf-project-init` Pasos 3/6/7/9/10 reescritos (install único desde raíz, sin `mkdir <fase>/`, crea los directorios de `artifacts`, template con docs lazy y tabla de entrypoints por doc de fase); hook `init-incomplete` verifica `.claude/phases/<fase>.md` con fallback al layout legacy (`<fase>/.claude/CLAUDE.md`) — proyectos antiguos siguen válidos; `--prune` reconcilia también los docs de fase huérfanos. Verificado: install completo (52 skills, 9 agents, 5 docs de fase), overlay KMM sin pisar raíz, hook en ambos layouts, prune con docs.* **Refinamiento (mismo día):** *los docs de fase pasaron de `.claude/phases/` (lazy por instrucción — dependía de que el modelo obedeciera) a `.claude/rules/sdd-<fase>.md` con frontmatter `paths:` — carga perezosa NATIVA del harness (verificado en doc oficial de Claude Code: path-specific rules). Globs por defecto = dir canónico + patrones por nombre de artefacto (`**/*_spec.md`, `**/*_plan.md`... — independientes del layout); `wf-project-init` ajusta el glob de directorio según `artifacts`. La regla KMM carga al tocar `**/*.kt(s)` o plan/tasks. De camino se cazó y arregló un bug latente de `install.sh`: `has_phase()` sin `local p` clobbereaba la variable de los bucles llamantes. Hook con fallback a los DOS layouts legacy.* **Afinado de capas (mismo día, a petición del usuario):** *restaurado el contrato de conocimiento por capas — (a) el CLAUDE.md raíz generado ya NO nombra workflows de fase (columna "Primer paso" eliminada; el routing inicial descansa en los `when_to_use` de las wf y el rootmap de cada fase vive en su regla); (b) eliminadas las tablas de kb-\* de los 5 CLAUDE.md de fase (prd/spec/design/plan/kmm — eran conocimiento del agente filtrado al contexto del orquestador y fuente de drift ya cazada por el audit: "11 kbs" vs 15 reales); el wiring kb↔agente tiene una sola SSoT: el frontmatter de los agentes (+ registry para inventario). Las rules quedan en 74-174 líneas. Residual honesto documentado en 9.9: las descriptions de kb-* siguen entrando eager en la lista de skills del orquestador.*
- [x] 🟠 **10.4 — Subcarpetas por fase dentro de cada feature.** *(Decidido con el usuario 2026-06-05.)* `features/<nombre>/` hoy es plano (spec, README, flows, views, ui_prompt, plan, tasks, deltas mezclados). → Estructura interna: `features/<n>/spec/`, `design/`, `plan/`, `tasks/` (README en la raíz de la feature). La co-localización que exigen sellador/gates/trazabilidad se mantiene (rutas relativas cortas tipo `../spec/x_spec.md`). Tocar convenciones de ruta en: `wf-spec-fast-track`, `wf-design-feature-prototype`, `wf-prepare-plan`, `wf-prepare-tasks`, `wf-spec-conflict`, `wf-spec-readiness`, búsquedas "junto al spec" de design — todas con fallback al layout plano (legacy). El sellador y los gates no cambian (resuelven desde headers/args). ✅ *2026-06-05: regla canónica aplicada en todas las piezas — lectura: subcarpeta de fase primero, raíz de la feature como fallback; escritura: feature nueva → subcarpetas, feature plana existente → sigue plana (no mezclar); raíz del producto = "el directorio que contiene `features/`" (sustituye a contar "dos niveles arriba", que se rompía con el nivel extra). **Escritores**: fast-track (spec → `spec/`, README en raíz + template con rutas por subcarpeta), feature-prototype/discover/moodboard/feedback/variant (→ `design/`), prepare-plan (→ `plan/`, + verificación de que `Spec origen` resuelve desde la ubicación final: `../spec/<n>_spec.md`), prepare-tasks (→ `tasks/`), sync-from-prd (junto al spec, agnóstico). **Lectores**: conflict y readiness escanean ambos layouts (`features/*/spec/*_spec.md` y `features/*/*_spec.md`); features-first detecta specs preexistentes en ambos; delta y su guía de trazabilidad; prepare-plan resuelve flows/views en `design/` con fallback y `DESIGN.md`/`DESIGN_BRIEF.md` vía `artifacts.design` → contenedor de `features/` → mismo dir (de paso se cerró un hueco del 10.1: feature-prototype y prepare-plan no consultaban `artifacts.design`). **Infra**: globs de rules en `install.sh` ampliados con `**/features/*/spec/**` y `**/features/*/design/**` (cubren los artefactos secundarios — deltas, sync_requirements, conflict_report, moodboard — que ningún glob por nombre cazaba); layout documentado como SSoT en la plantilla de `wf-project-init` (árbol completo + regla de no mezclar) y reflejado en CLAUDE.md raíz, READMEs de spec/plan, DIAGRAMS y ejemplos. Verificado: `sdd-seal.py` resuelve `Spec origen: ../spec/login_spec.md` desde `features/login/plan/` (check "Spec origen legible" ✓ en test real); `bash -n install.sh` OK; las rutas `features/<feature>/di/` del overlay KMM son módulos de código Kotlin, no artefactos — intactas.*
- [x] 🟠 **10.2 — Topología multi-repo: specs como repo SSoT compartido.** Un producto real puede ser N repos: uno SSoT con PRD+specs (+design) + repos de servidor, app y web que consumen los mismos specs. Hoy todo asume monorepo (artefactos y código juntos); no hay forma de que `wf-prepare-plan`/`wf-prepare-tasks` en el repo del servidor lean specs de otro repo. → Soportar en `project-init.json` un `artifacts_source` (path local al checkout del repo SSoT, p. ej. sibling o submodule) del que los gates y workflows de plan/tasks resuelven los specs. **Reparto de instalación** *(concretado con el usuario 2026-06-05)*: el repo SSoT instala solo las fases de autoría (`prd`, `spec`, `design`) con sus workflows y agentes; los repos consumidores instalan solo `plan`, `tasks` + overlay de stack — se ahorran los ~12 wf-spec-\*, 4 agentes spec y 3 piezas de prd (las kb cross-fase de lectura, `kb-spec-expert`/`kb-a11y-expert`, ya las trae `install.sh plan` como dependencia). Implica: (a) **relajar el backbone fijo de `wf-project-init`** ("spec, plan y tasks SIEMPRE" — Regla 4): hace falta un perfil/modo "consumidor" que instale `plan,tasks` sin autoría de spec, apuntando a `artifacts_source`; (b) **los `_plan.md`/`_tasks.md` viven en el repo consumidor** (cada consumidor tiene SU plan de la misma feature; backend y app planifican distinto el mismo spec), con `Spec origen` apuntando al checkout del SSoT — el sellador ya resuelve relativo al plan, funciona con sibling/submodule; (c) pin de versión/commit de los specs consumidos por repo (se conecta con 5.1 versionado y 4.x detección de deriva); (d) el hook de sesión debe entender ambos tipos de repo; (e) las rules de fase del consumidor no incluyen `sdd-prd.md`/`sdd-spec.md`. Relacionado con 5.7 (monorepo) y 6.1 (multi-stack) — las tres son variantes del mismo problema: "la unidad de proyecto no es un repo". ✅ *2026-06-07: (a) `wf-project-init` pregunta 5.0b nueva "Topología" (solo perfil dev): specs propios (standalone, comportamiento de siempre) vs consume specs de otro repo → `TOPOLOGY=consumer`, pide y VALIDA el path local al checkout del SSoT (sibling o submodule: existe + contiene specs), salta las preguntas de autoría (PRD/Diseño/Artefactos) y mantiene la entrevista técnica; `SELECTED_PHASES=[plan,tasks]` (backbone fijo relajado — punto (a) del ítem); schema con `artifacts_source` + `artifacts_source_pin` (commit del SSoT al init — punto (c), apoyado en el versionado del 5.1); template de CLAUDE.md raíz con la variante "Topología: repo consumidor" (specs solo lectura, cambios de spec → repo SSoT). (b) `wf-prepare-plan`: en repos consumidores el plan se escribe en ESTE repo (`features/<n>/plan/`, nunca junto al spec del SSoT), `Spec origen` con la ruta relativa cross-repo, y aviso no bloqueante si el HEAD del SSoT difiere del pin (paso 5 nuevo de verificación). `wf-prepare-tasks`/`wf-task-run`/`wf-bug` funcionan sin cambios (operan sobre plan/tasks locales). (d) el hook ya entiende ambos tipos sin cambios (consumer = proyecto con phases [plan,tasks], sus rules existen) y (e) las rules de autoría no se instalan porque `install.sh plan,tasks` no las trae — verificado que `install.sh plan` ya trae `kb-spec-expert`/`kb-a11y-expert` como dependencias (líneas 184-186). Verificado end-to-end con dos repos simulados: sellador VALIDA y sella un plan del consumidor con `Spec origen` cruzando al checkout sibling del SSoT, y el gate permite el path cross-repo.*

## FASE 11 — Validación end-to-end del ecosistema *(añadida 2026-06-07, a revisar en la fase de testing)*

Puntos a verificar cuando se pruebe el ecosistema en proyectos reales — no son implementación nueva, son auditoría del comportamiento real de lo ya construido.

- [ ] 🟠 **11.1 — Paso de argumentos en la cadena orquestador → skill → agente.** Las skills NO están pensadas para que el usuario las ejecute directamente por terminal con el comando y sus flags: el usuario escribe texto normal y el orquestador/agente subdelega construyendo los argumentos. → Verificar en uso real que (a) el orquestador construye correctamente los `args` de cada `wf-*` a partir de lenguaje natural (rutas resueltas, flags como `--features F-001,F-002`, modos `analyze|apply|generate`); (b) cuando una wf delega a otra wf o a un subagente (p. ej. `wf-spec-features-first` → `Agent(sdd-spec-writer, "Ejecuta /wf-spec-fast-track con ...")`), los argumentos sobreviven el salto y el subagente recibe rutas/flags válidos; (c) los `argument-hint` y `when_to_use` son suficientes para que el matching semántico acierte sin que el usuario conozca la sintaxis; (d) los gates PreToolUse (que parsean `args` por regex) reciben los args en el formato que esperan también cuando la invocación la construye un agente y no un humano.
- [ ] 🟠 **11.2 — Inventario de prosa sustituible por scripts deterministas.** El patrón sellador/gates/task-state (autor ≠ verificador mecánico) ya demostró que la prosa "verifica que X" es el punto débil. → Auditar TODOS los puntos del flujo que hoy dependen de que el agente obedezca prosa y evaluar cuáles migrar a script. Candidatos detectados durante la implementación 2026-06-05/07: resolución de layout (`artifacts.*` + subcarpetas de feature + fallbacks — hoy es una frase canónica repetida en ~15 skills; un `sdd-resolve-path.py` la haría única y testeable); ~~consolidación incremental de `_features.md` (features-first Paso 6 — merge por prosa, riesgo de pérdida de estados)~~ ✅ *cerrado por 5.4 (2026-06-08): `_features.md` es ahora índice generado por `sdd-features-index.py`*; detección de specs/artefactos preexistentes en ambos layouts; verificación de headers de trazabilidad al generar artefactos (Spec origen relativo correcto — hoy se pide por prosa en prepare-plan Paso 7); numeración secuencial de IDs (B-00X, F-C-00X, T-00X — colisiones si se generan por prosa); checks del Paso 9 de `wf-project-init`; chequeo estructural de frontmatter del meta-ecosistema (ya anotado como 9.8); validación de que los globs de las rules cubren los `artifacts` declarados tras el init. Criterio de migración: si el fallo de la prosa corrompe estado o trazabilidad → script; si solo degrada calidad de texto → prosa.
- [ ] 🟡 **11.3 — Suite de tests del ecosistema (mejora/optimización final).** Hoy la verificación es manual y ad-hoc (tmpdirs improvisados en cada sesión de implementación). → Evaluar qué partes del sistema son testeables de forma automatizada y montar la suite: (a) **scripts deterministas** (`sdd-seal.py`, `sdd-gate-check.py`, `sdd-task-state.py`, `merge-claude-settings.py`, `generate-skill-registry.py`) — unit tests puros con fixtures de artefactos (plan sellable/no sellable, tasks con deps, specs con cada marcador), son los más baratos y los que protegen estado; (b) **installers** (`install.sh`, `tech/*/install.sh`, `setup.sh`) — tests de integración en tmpdir: instalación por fase, overlay sin pisar raíz, `--prune`, idempotencia del merge de settings, layouts legacy; (c) **hook de sesión** (`sdd-session-check.sh`) — matriz de estados (mode-undecided/init-pending/init-incomplete/sin directiva) sobre proyectos sintéticos; (d) **flujos con LLM** (lo difícil): evaluar smoke-tests tipo golden-path sobre un proyecto fixture (¿el discover encuentra las capacidades plantadas?, ¿el gate deniega el spec con [INFERIDO]?) asumiendo no-determinismo — quizá como checklist semiautomática más que CI estricta. Ejecutable local y como pre-commit/CI (conecta con 9.8 y 5.x).
- [ ] 🟡 **11.4 — Robustez del tooling meta de ESTE repo (mejora/optimización final).** El `.claude/` del repo del ecosistema (meta-orquestador + `wf-skill-create`/`wf-agent-create`/`wf-stack-create`/`wf-sdd-refactor`/`wf-sdd-audit`) es lo que usamos para crear componentes nuevos, y hoy confía en prosa: nada impide crear una skill con frontmatter inválido, nombre duplicado, kb sin agente que la cargue, wf sin entrada en rootmap, o referencias `${CLAUDE_SKILL_DIR}` rotas. → Endurecer el ciclo de creación: (a) validador estructural determinista (script) que los workflows de creación/refactor ejecuten al cerrar — frontmatter completo según el canon de `kb-sdd-creation-guide`, nombre único contra el registry, kb referenciada por ≥1 agente o declarada huérfana consciente, wf con `when_to_use`/`argument-hint`, paths de references existentes; (b) regenerado de registry + chequeo de drift de rootmaps como paso obligatorio (hoy es prosa en los workflows); (c) evaluar hook PreToolUse/PostToolUse en el `.claude/` de este repo que corra el validador sobre `Write/Edit` en `*/SKILL.md` y `*/agents/*.md`; (d) plantillas ejecutables (scaffold) en vez de "sigue el template" por prosa. Se apoya en 11.3 (el validador ES testeable) y absorbe 9.8.
- [ ] 🔴 **11.5 — Documentación completa del ecosistema (LO ÚLTIMO DE TODO — cuando el resto de cambios estén implementados).** *(Pedido por el usuario 2026-06-07.)* Dos documentos complementarios, escritos para dos lectores: el propio autor releyendo el sistema en frío, y un contribuidor futuro que necesita conocer el ecosistema al detalle sin arqueología de git ni leer 111 skills.
  - **(a) Explicación FUNCIONAL del sistema completo**: premisas y filosofía (spec-driven, anti-alucinación, autor≠sellador, conocimiento por capas); qué fases existe y qué implementa cada una (PRD → Spec → Design → Plan → Tasks + ejecución/mantenimiento); los subpaths de flujo reales (greenfield vía PRD, brownfield vía characterization, iteración por subsets, cambio de producto vía prd-change/delta/sync, bugs vía triaje, multi-stack vía overlay); **todas las decisiones de diseño tomadas y su porqué** (gates deterministas, [INFERIDO] bloquea, layout de features, infra única con rules lazy, registro por feature...); y el mapa explícito de **cuándo entra el humano a decidir** (wizard de modo, entrevista de init, gaps críticos, mapa de discovery, triaje de bug, confirmación de inferidos, sellados/validaciones, política de commits) — el contrato humano-sistema completo.
  - **(b) Explicación TÉCNICA de diseño IA**: por qué cada conocimiento vive donde vive (CLAUDE.md vs rule vs kb vs wf vs agente — el contrato de capas y el coste de contexto de cada decisión); mecánica de carga (eager vs lazy, `paths:` de rules, frontmatter `skills:` de agentes, descriptions como único coste fijo); resolución de paths y layouts (artifacts map, subcarpetas, fallbacks legacy, raíz = contenedor de features/); arquitectura de enforcement (hooks PreToolUse, sellador, task-state, qué es prosa y qué es script y el criterio de corte); contrato de overlay (sustitución por basename, orden base→overlay); versionado y distribución (por copia, sellos, deriva); y las trampas conocidas a tener en cuenta (macOS pwd -P, ugrep vs grep, redistribución de templates compartidos, `local` en bash...).
  - Formato a decidir al hacerlo (docs/ con índice, o ARCHITECTURE.md + FUNCTIONAL.md); debe quedar enlazado desde el README raíz y mantenerse como parte del ciclo de cambios (un cambio de diseño sin su actualización de doc = incompleto). Se apoya en el ROADMAP completado como registro histórico de decisiones.

- [ ] 🔴 **11.6 — Playbook de testeo manual del ecosistema (AL FINAL DE TODO — con el roadmap completo).** *(Pedido por el usuario 2026-06-07.)* Guía de casos de prueba para que el USUARIO valide el ecosistema a mano en proyectos reales — complementa el 11.3 (automatizable) con lo que solo un humano puede juzgar. Cada caso con formato fijo: **Objetivo** (qué se quiere conseguir/verificar), **Qué debería hacer el agente** (comportamiento esperado paso a paso, incluyendo dónde debe detenerse y preguntar), **Salida esperada** (artefactos generados con sus paths y contenido mínimo, mensajes al usuario, estados sellados). Cobertura mínima de casos a redactar:
  - **Arranque**: proyecto virgen → wizard de modo (SDD/libre); modo libre → silencio para siempre; init con entrevista por perfil; init interrumpido → init-incomplete repara; deriva de versión → aviso de una línea sin bloquear.
  - **Greenfield completo**: de notas → PRD → analyze (responder/ignorar gaps) → features-first con subset → design (intake→system→prototype) → plan → validate/sellado → tasks → task-run hasta COMPLETO con commits trazables.
  - **Brownfield**: spec-from-code discover (¿encuentra lo que el código evidencia? ¿confianzas razonables?) → gate humano → generate → [INFERIDO] bloqueando plan → gap-resolve confirmando → plan desbloqueado.
  - **Gates en negativo (anti-alucinación, los más importantes)**: pedir explícitamente saltarse pasos ("hazme las tasks sin validar el plan", "genera el plan aunque haya CRÍTICOs") y verificar que el agente NO cede y el hook deniega; editar un Estado a mano y comprobar que el sellador lo detecta.
  - **Mantenimiento**: bug real → triaje correcto en las 3 categorías (CODE_BUG/SPEC_CHANGE/UNSPEC) con casos preparados de cada una; cambio de producto → prd-change → sync-impact → specs resincronizados.
  - **Lenguaje natural (conecta con 11.1)**: todas las pruebas se lanzan HABLANDO, nunca tecleando comandos `/wf-*` — verificar que el routing acierta la skill y construye los args.
  - **Robustez**: update de versión sobre proyecto con overlay (¿sobreviven las variantes de stack?); reinstalación con --prune; proyecto con layout legacy plano (¿los workflows lo leen sin migrar?); sesión en subdirectorio del proyecto.
  - Resultado de cada caso: PASS/FAIL + notas → los FAIL alimentan nuevos ítems de este ROADMAP. Vive en `docs/` junto a la documentación del 11.5 y se redacta después de ella (la doc funcional define el comportamiento esperado; el playbook lo convierte en casos ejecutables).
| Prioridad | Qué | Por qué primero |
|---|---|---|
| 1 | **Fase 0** completa ✅ | Bugs reproducibles, bajo esfuerzo, varios "roto de fábrica" |
| 2 | **1.1 + 1.2** (enforcement de gates) | Es el riesgo central: gates auto-aprobables = alucinación sin detección. Además, `wf-task-run` (2.1) se construye sobre esta infraestructura de sellado determinista — hacerla primero evita migrarlo después *(orden invertido con la fila 3 el 2026-06-05)* |
| 3 | **10.1** (contrato de ubicación de artefactos) | Mal comportamiento ya observado en prueba real (artefactos de spec creados junto al PRD ignorando `specs/`); condiciona dónde leen/escriben los gates de 1.x y todas las piezas nuevas — cuanto más tarde, más rutas hardcodeadas que migrar |
| 3b | **10.3 + 10.4** (infra única + subcarpetas de feature) ✅ | Decidido con el usuario; debe ir ANTES de wf-bug/wf-task-run para que las piezas nuevas nazcan con el layout definitivo |
| 4 | **2.2** (`wf-bug`) + **2.1** (`wf-task-run`) ✅ | Sin ciclo de vida real, el sistema muere al primer sprint de mantenimiento |
| 5 | **3.1** (`wf-spec-from-code`) ✅ | Abre todo el mercado brownfield — la mayoría del trabajo real |
| 6 | **5.1** (versionado + update) ✅ | Prerequisito para usar el ecosistema en >1 repo / >1 dev |
| 7 | **4.1** (modo ligero) + **10.2** (multi-repo) ✅ | Adopción en scrum real y topologías de producto multi-repo |
| 8 | **7.1** (declarar fit de design) ✅ | Barato y elimina falsas expectativas |
| 9 | Resto por severidad | — |

---

## Completado

*(mover aquí los ítems al cerrarlos, con fecha y commit)*

- [x] **Skill Registry** (`meta/skill-registry.md` + Regla 18) — *IMPROVEMENTS #1, previo a 2026-06-05* — ⚠️ pendiente de fiabilidad: ver 0.5
- [x] **Token budget / criterios de densidad** en `kb-sdd-creation-guide` — *IMPROVEMENTS #3, commit 985bd5d*
- [x] **`wf-kmm-init` + `kmm_project_state.md`** — *IMPROVEMENTS #2* — ⚠️ el installer no despliega la KB del protocolo: ver 0.1
- [x] **Contrato de overlay + `wf-stack-create`** (A1+A2 del backlog de IMPROVEMENTS)
- [x] **`model: claude-opus-4-6` no canónico en `wf-spec-features-first`** — re-verificado 2026-06-05: campo eliminado
- [x] **`kb-plan-expert` movida a `plan/skills/`** — re-verificado 2026-06-05: existe la genérica en `plan/` y la variante KMM como override (conforme al contrato de overlay)
- [x] **`when_to_use` en kb-*: limpieza principal** — de 37 a 4 ficheros (resto: ver 9.4)
