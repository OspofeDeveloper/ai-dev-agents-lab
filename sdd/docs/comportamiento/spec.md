# Generación de specs — `EC-SPEC`

!!! warning "Área en construcción"
    Casos de la fase Spec. Cobertura prevista: `wf-spec-analyze` obligatorio y su
    parada por gaps `[CRÍTICO]`; mapa de discovery y elección de subset (features
    no incluidas → `PENDIENTE_GENERACIÓN`); `wf-spec-features-first` en paralelo;
    gobernanza de expansión de alcance desde el analysis; characterization brownfield
    (`wf-spec-from-code`: gate humano tras discover, `[INFERIDO]` por falta de
    evidencia); CAs en formato `GIVEN/WHEN/THEN`; modo ligero (proporcionalidad sin
    relajar invariantes).
