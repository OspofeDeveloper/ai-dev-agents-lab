# Fase Design — `EC-DSGN`

!!! warning "Área en construcción"
    Casos de la fase Design. Cobertura prevista: Design saltable cuando no hay UI;
    `wf-design-intake` como gate obligatorio (no hay `DESIGN.md` sin brief cerrado);
    `wf-design-system` y `wf-design-feature-prototype` (flows/views/ui_prompt con
    todos los estados); onramp brownfield `wf-design-extract` (gate humano tras
    discover, `origin: extracted`, inconsistencias documentadas no promediadas);
    `wf-design-sync` para detectar artefactos stale.
