# Mantenimiento y cambios — `EC-MAINT`

!!! warning "Área en construcción"
    Casos del trabajo post-entrega. Cobertura prevista: triaje de `wf-bug` en las 3
    categorías con casos preparados de cada una (`CODE_BUG` no toca el spec /
    `SPEC_CHANGE` no toca código → delta / `UNSPEC` formaliza antes); cambio de
    producto (`wf-prd-change` → `wf-prd-sync-impact` → `wf-spec-sync-from-prd`) y la
    cascada (`wf-prd-change-cascade` parando solo en los 4 checkpoints humanos);
    back-edge de enmienda (`wf-spec-amend`: solo aclaración, el cambio de
    comportamiento escala a `wf-spec-delta`).
